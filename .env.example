# Get this from cloud.umami.is after adding your site (Settings > Websites).
# It's not a secret, but keeping it in an env var means you don't hardcode
# it into the repo and can swap it per-environment.
PUBLIC_UMAMI_WEBSITE_ID=
